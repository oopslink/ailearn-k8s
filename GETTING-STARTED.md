# 快速开始指南

欢迎使用 Kubernetes AI 学习仓库！本指南将帮助你开始你的 Kubernetes 学习之旅。

## 前置准备

### 1. 安装 Claude Code

确保你已经安装了 Claude Code CLI：

```bash
# 访问 https://claude.com/claude-code 获取安装说明
```

### 2. 设置本地 Kubernetes 环境（推荐但非必需）

选择以下任一工具在本地运行 Kubernetes：

**Minikube** (推荐初学者):
```bash
# macOS
brew install minikube
minikube start

# Linux
curl -LO https://storage.googleapis.com/minikube/releases/latest/minikube-linux-amd64
sudo install minikube-linux-amd64 /usr/local/bin/minikube
minikube start
```

**Kind** (Kubernetes in Docker):
```bash
# macOS/Linux
brew install kind
# 或
GO111MODULE="on" go install sigs.k8s.io/kind@latest

# 创建集群
kind create cluster
```

**K3d** (轻量级):
```bash
brew install k3d
k3d cluster create mycluster
```

### 3. 安装 kubectl

```bash
# macOS
brew install kubectl

# Linux
curl -LO "https://dl.k8s.io/release/$(curl -L -s https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl"
sudo install -o root -g root -m 0755 kubectl /usr/local/bin/kubectl

# 验证安装
kubectl version --client
```

## 开始学习

### 第一步：启动 Claude Code

在此仓库目录中打开终端并运行：

```bash
claude-code
```

### 第二步：提出你的第一个问题

Claude Code 启动后，你可以开始提问。以下是一些建议的起始问题：

#### 如果你是 Kubernetes 新手：
```
"我是 Kubernetes 新手，能给我介绍一下什么是 Kubernetes 吗？"
```

```
"什么是 Pod？为什么它是 Kubernetes 的基础单元？"
```

```
"我应该如何开始学习 Kubernetes？有什么建议的学习路径吗？"
```

#### 如果你有一些经验：
```
"Deployment 和 StatefulSet 的区别是什么？我应该在什么情况下使用它们？"
```

```
"如何在 Kubernetes 中配置持久化存储？"
```

```
"解释一下 Kubernetes 的服务发现机制"
```

#### 如果你想要实践：
```
"能教我如何创建一个简单的 nginx Deployment 吗？"
```

```
"我想学习如何使用 kubectl 命令来管理 Pods"
```

```
"如何调试一个无法启动的 Pod？"
```

### 第三步：跟随 Claude 的引导

Claude 会：
1. 先询问你已经知道什么
2. 基于你的现有知识提供解释
3. 通过问题检查你的理解
4. 提供实践命令和 YAML 示例
5. 自动记录你的学习进度

## 学习建议

### 建议的学习顺序

1. **第 1-2 周：Kubernetes 基础**
   - 理解 K8s 架构
   - 学习核心概念（Pod、Node、Namespace）
   - 掌握基本 kubectl 命令
   - 练习编写简单的 YAML 配置

2. **第 3-4 周：工作负载管理**
   - Deployments 和滚动更新
   - StatefulSets 用于有状态应用
   - Jobs 和 CronJobs
   - 实践：部署多个应用

3. **第 5 周：服务和网络**
   - Services 和服务类型
   - Ingress 配置
   - Network Policies
   - 实践：设置应用间通信

4. **第 6 周：配置和存储**
   - ConfigMaps 和 Secrets
   - Persistent Volumes
   - Storage Classes
   - 实践：添加持久化存储

5. **第 7-8 周：高级主题**
   - RBAC 和安全
   - 资源管理和调度
   - 监控和故障排查
   - Helm 和 Operators

### 学习技巧

1. **实践为主**: 每学习一个新概念，立即在你的本地集群上实践
2. **记录笔记**: Claude 会自动记录，但你也可以保留自己的笔记
3. **定期复习**: 使用进度追踪器查看需要复习的主题
4. **提出问题**: 不要害怕提问 - Claude 采用无评判的教学方式
5. **解决错误**: 当遇到错误时，让 Claude 帮你调试 - 这是最好的学习方式

## 查看你的进度

### 检查整体进度

查看 `/progress/k8s-study-tracker.md` 文件：

```bash
cat progress/k8s-study-tracker.md
```

或在 Claude Code 中询问：
```
"显示我的学习进度"
```

### 查看历史会话

所有学习会话都保存在 `/sessions/` 目录中，按日期组织：

```bash
ls sessions/
cat sessions/2026-01-20/session-notes.md
```

## 复习和测试

### 复习薄弱领域

在 Claude Code 中询问：
```
"让我们复习一下我遇到困难的主题"
```

```
"在我的薄弱领域测试我"
```

### 获取学习建议

```
"今天我应该专注学习什么？"
```

```
"基于我的进度，我接下来应该学什么？"
```

## 认证准备

如果你的目标是获得 Kubernetes 认证（CKA、CKAD、CKS），告诉 Claude：

```
"我想准备 CKA 认证考试，你能帮我制定学习计划吗？"
```

Claude 会调整教学重点，专注于：
- 考试相关的主题和权重
- 命令行效率（imperative commands）
- 时间管理技巧
- 实践场景

## 实用 kubectl 速查

这些是最常用的命令，你会经常使用：

```bash
# 查看资源
kubectl get pods
kubectl get deployments
kubectl get services
kubectl get all

# 详细信息
kubectl describe pod <pod-name>
kubectl describe deployment <deployment-name>

# 日志
kubectl logs <pod-name>
kubectl logs -f <pod-name>  # 实时日志

# 交互式调试
kubectl exec -it <pod-name> -- /bin/bash
kubectl debug <pod-name> -it --image=busybox

# 应用配置
kubectl apply -f <file.yaml>
kubectl delete -f <file.yaml>

# 快速创建（imperative）
kubectl create deployment nginx --image=nginx
kubectl expose deployment nginx --port=80 --type=LoadBalancer

# 编辑资源
kubectl edit deployment <deployment-name>

# 查看事件
kubectl get events --sort-by='.lastTimestamp'
```

## 常见问题

### Q: 我需要有编程经验吗？
A: 不需要，但基本的命令行知识会有帮助。

### Q: 我需要购买云服务吗？
A: 不需要，你可以使用免费的本地工具（Minikube、Kind、K3d）。

### Q: 学习需要多长时间？
A: 取决于你的目标：
- 基础理解：2-4 周
- 中级应用：2-3 个月
- 认证准备（CKA/CKAD）：3-6 个月

### Q: Claude 会自动保存我的进度吗？
A: 是的！每次会话后，Claude 会自动更新你的进度追踪器和会话记录。

### Q: 我可以暂停学习后再继续吗？
A: 当然可以。当你回来时，Claude 可以查看你的学习历史，从你离开的地方继续。

## 获取帮助

如果遇到问题：

1. **在 Claude Code 中询问**: Claude 可以帮助解决大多数学习和技术问题
2. **查看官方文档**: https://kubernetes.io/docs/
3. **提交 Issue**: 在 GitHub 仓库提交问题

## 下一步

现在你已经准备好了！启动 Claude Code 并提出你的第一个问题吧。

```bash
claude-code
```

祝学习愉快！🚀

---

**记住**: 学习 Kubernetes 是一个旅程，不是冲刺。慢慢来，多实践，不要害怕犯错。每个错误都是学习的机会！
